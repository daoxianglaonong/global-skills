# 500ms 品牌归属判断 · 评分表

| 项 | 值 |
| --- | --- |
| style-set | `{{STYLE_SET_ID}}` |
| accept-run-id | `{{RUN_ID}}` |
| 页面类型（整组同一，Q-32） | `{{PAGE_TYPE}}` |
| 配置曝光时长 | `{{EXPOSURE_MS}}` ms（实际所用毫秒必须以裁判端实测为准，Q-23） |
| 随机化种子 | `{{SEED}}` |
| 裁判代号 | |
| 主持人 | |
| 日期 | |

## 1. 裁判资格（Q-26–Q-28，四项全勾才可计分）

- [ ] 完全未参与本项目
- [ ] 未看过本站资产
- [ ] 未参与提取
- [ ] 未参与拍板

拉不到符合条件的裁判，试点必须不通过；**不设降级通道**，不得改由提取者、同事、用户本人或视觉 LLM 顶替计分（Q-28）。提取者只主持与记旁注，票不计分（Q-27）。

## 2. 学习与校准（Q-29–Q-31）

- [ ] 学习材料 3 张（首页 + 2 张内页）静态视口图，各 10 秒，全程未自由浏览原站
- [ ] 校准题第 1 轮：□ 通过 □ 未过
- [ ] 未过则重看一轮学习材料后再测一题；第 2 轮：□ 通过 □ 未过
- [ ] 两轮仍错 → 换裁判，本轮不作数，**不得**因此放宽 Q-40

## 3. 第 1 轮（遮罩轮，计分；Q-37）

按 `manifest.json` 的 `maskedOrder` 逐张记录。`itemId` 是不透明 id，主持人事后按 `answerKey` 还原来源。

| itemId | 判定（目标站 / 其他站 / 不确定） | 凭什么（原话） | 实测曝光 ms |
| --- | --- | --- | --- |
{{ITEM_ROWS}}

## 4. 第 2 轮（原样轮，不计分，只作归因；Q-37）

同表另记。原样轮**不得**回改第 1 轮判定。

## 5. 「凭什么」人判（Q-40 第 4 款）

对每条被判「目标站」的生成页，由**人**判断其理由是否**只**指向已遮蔽的身份标识（logo / 品牌名 / 产品名 / 备案号 / 电话）。

| itemId | 只指向身份标识？（是 / 否） | 判定人 |
| --- | --- | --- |
{{ITEM_ROWS}}

此列**必须由人填**。本仓不提供任何模型代填路径（Q-45 / Q-46）。填成 `answers.json` 的 `whyReview` 字段后交 `score-attribution.mjs`。

## 6. 描述性判据（Q-40，四款全满足才算风格级通过）

1. □ 校准题已通过
2. □ 生成页**未被全部**标成「其他站」
3. □ 竞品页**未被全部**标成「目标站」（全部标成 → 判材料失败，重做材料，**不算资产失败**）
4. □ 「凭什么」的回答不只指向已遮蔽的身份标识

**N=1 无统计功效，本级为描述性判据。** 禁止写 3/4、1/4 或任何生成页正确率数字闸门（Q-41）；数字闸门只留到 N≥10 的多裁判场景（Q-42）。

## 7. 本次材料备注

{{NOTES}}
